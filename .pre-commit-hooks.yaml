- id: format-precice-config
  name: format preCICE configuration files
  description: applies the cannonical format for preCICE configuration files
  entry: format-precice-config
  language: python
  types: ["file", "text"]
  files: \.xml$
  require_serial: false
- id: check-image-prefix
  name: checks image prefix for website integration
  description: checks images to have the given prefix  
  entry: check-image-prefix
  language: python
  types: ["file", "image"]
  files: ".*/images/.*"
  require_serial: false
